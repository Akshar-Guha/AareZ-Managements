global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'healthcare-manager'
    static_configs:
      - targets: ['localhost:3000']  # Adjust this to match your app's metrics endpoint
    metrics_path: '/metrics'
    scheme: 'http'

  # Optional: Add more services to monitor
  # - job_name: 'node_exporter'
  #   static_configs:
  #     - targets: ['localhost:9100']
